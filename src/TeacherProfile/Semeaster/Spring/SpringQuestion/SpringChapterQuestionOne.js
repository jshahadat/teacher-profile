import React from 'react';

const SpringChapterQuestionOne = ({ chapterOne }) => {
    return (
        <div>
            <h1>{chapterOne.question}</h1>
        </div>
    );
};

export default SpringChapterQuestionOne;